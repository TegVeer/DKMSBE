'use strict';

/**
 * inventory-transaction router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::inventory-transaction.inventory-transaction');
